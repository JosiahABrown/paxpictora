---
// Import Material Symbols stylesheet in the layout, not the component
---
<nav class="bg-surface/90 backdrop-blur-sm font-manrope uppercase tracking-widest fixed top-0 left-0 w-full z-50 flex justify-between items-center px-margin-edge py-6 border-b border-surface-variant">
    <div class="text-title-sm font-playfair text-on-surface">
        Pax Pictora
    </div>
    <div class="hidden md:flex gap-8 items-center text-on-surface-variant">
        <a class="hover:text-primary transition-colors px-4 py-2" href="#">Portfolio</a>
        <a class="text-primary border-b border-primary transition-colors px-4 py-2" href="#">About</a>
        <a class="hover:text-primary transition-colors px-4 py-2" href="#">Contact</a>
    </div>
    <button class="bg-primary text-on-primary rounded-xl px-6 py-3 font-manrope font-semibold hover:bg-inverse-surface transition-colors hidden md:block">
        Book Session
    </button>
</nav>

<style>
  /* Define local utility font families if not in global configuration */
  .font-playfair { font-family: var(--font-family-serif); }
  .font-manrope { font-family: var(--font-family-body); }
</style>